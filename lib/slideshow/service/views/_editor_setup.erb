<script>

    $(document).ready( function () {
      
      /* NB: we need to use to_json (to escape newlines in string if present etc.)
      */
 
       markdown_note_new( {
         welcome: <%= @welcome_sample.to_json %>   
       } );
      
    });


/*  todo: check if loading image w/ relative path works  ??
*/

</script>
